Algoritmo "consumo"

Var

dist, comb, consmed : real

Inicio

escreva("Distancia percorrida: ")
leia(dist)
escreva("Combustível gasto: ")
leia(comb)

consmed <- dist / comb

escreva("Consumo médio = ", consmed:8:3)

Fimalgoritmo