Algoritmo "tabuada"

Var

   n, i, r : inteiro

Inicio

   escreva("Deseja a tabuada para qual valor? ")
   leia(n)

   para i de 1 ate 10 faca
      r <- n * i
      escreval(n," X ", i, " = ", r)
   fimpara
   
Fimalgoritmo